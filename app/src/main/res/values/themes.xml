<resources>
    <style name="Theme.Pizzetos" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Hacer la barra de estado transparente -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Cambiar el color de fondo de la barra de navegación -->
        <item name="android:navigationBarColor">@color/white</item>
        <!-- Hacer que el texto en la barra de estado sea oscuro -->
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
